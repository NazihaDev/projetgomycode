

//partie like pour chaque book 
/*const mongoose = require('mongoose');

const LikesSChema  = new mongoose.Schema({
    idUser : {
        type: mongoose.Schema.Types.ObjectId,
        ref: "User",
        required: false,
      },
    idBook :{
        type: mongoose.Schema.Types.ObjectId,
        ref: "Book",
        required: false,
      }
});

const Like = mongoose.model("Like", LikesSChema);

module.exports = Like;*/